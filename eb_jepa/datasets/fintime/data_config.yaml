# FinTime dataset defaults (financial multivariate time series).
data_root: /lustre/work/pdl17890/udl806719/datasets/Finance/fintime_prep
in_channels: 87          # OHLCV + ~80 technical indicators + calendar (z-scored)
seq_len: 64              # daily steps per window
target: direction        # direction (2-cls up/down) | return (regression)
batch_size: 256
num_workers: 8
aug_noise_std: 0.05
aug_scale_jitter: 0.1
